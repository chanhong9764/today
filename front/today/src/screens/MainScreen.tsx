import { SafeAreaView } from 'react-native';
import Navigator from '../navigator/Stack';

function MainScreen() {
  return (
    <SafeAreaView>
      <Navigator />
    </SafeAreaView>
  );
}

export default MainScreen;
